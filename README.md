# Mermaid 在线编辑器

一个可部署到 Vercel 的 Mermaid 在线编辑器。支持实时预览，并可导出 `SVG`、`PNG`、`JPG`。

## 本地运行

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

Vercel 会使用 `npm run build` 构建，并部署 `dist` 目录。
